server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/SI657-2302-WX71-DigitAlholics/TheraphyMicroservices.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
          default-label: "master"
  security:
    user:
      name: root
      password: s3cr3t